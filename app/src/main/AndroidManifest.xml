<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:dist="http://schemas.android.com/apk/distribution"
    package="com.example.athleticssports">

    <dist:module dist:instant="true" />

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ViewTeam"></activity>
        <activity android:name=".StopWatch" >

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

        <service
            android:name=".MyFirebaseMessagingService"
            android:enabled="true"
            android:exported="true"
            android:permission="com.google.android.c2dm.permission.SEND">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
                <action android:name="com.google.android.c2dm.intent.RECEIVE" />
            </intent-filter>
        </service>

        <activity android:name=".AddData" />
        <activity android:name=".WelcomeActivity">

        </activity>
        <activity android:name=".Registration" />
        <activity android:name=".MyProfile" />
        <activity android:name=".Teams" />
        <activity android:name=".Update" />
        <activity android:name=".AthleteComparison" />
        <activity android:name=".Records" />
        <activity android:name=".Time">

            <!-- <action android:name="android.intent.action.VIEW"/> -->

        </activity>
        <activity android:name=".players" />
        <activity android:name=".RegisterTeam" />
        <activity android:name=".RegisterAthlete" />
        <activity android:name=".RegisterRace" />
        <activity android:name=".MainActivity" />
    </application>

</manifest>